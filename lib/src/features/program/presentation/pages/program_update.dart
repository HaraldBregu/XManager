import 'package:flutter/material.dart';

class ProgramUpdate extends StatelessWidget {
  const ProgramUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
