import 'package:flutter/material.dart';

class PlayerCreateView extends StatelessWidget {
  const PlayerCreateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
