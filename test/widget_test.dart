// This is a basic Flutter shared test.
//
// To perform an interaction with a shared in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the shared
// tree, read text, and verify that the values of shared properties are correct.

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:xmanager/main.dart';
import 'package:xmanager/views/responsive/responsive.dart';

void main() {
  testWidgets('Counter increments smoke test', (WidgetTester tester) async {
    // Build our app and trigger a frame.

  });
}
